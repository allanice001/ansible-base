---
galaxy_info:
  author: dragonmaster
  description: Simple base providing git, pip, wheel, virtualenv, various utils.
  license: BSD
  min_ansible_version: 2.0
  platforms:
    - name: Ubuntu
      versions:
      - trusty
      - xenial
  categories:
    - system
dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.

